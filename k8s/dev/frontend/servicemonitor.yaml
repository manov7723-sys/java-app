apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: java-app-frontend
  namespace: d1
  labels:
    app.kubernetes.io/name: java-app-frontend
    app.kubernetes.io/managed-by: deepagent
    release: monitoring
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: java-app-frontend
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
